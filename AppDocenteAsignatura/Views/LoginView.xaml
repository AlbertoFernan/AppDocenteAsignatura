<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppDocenteAsignatura.Views.LoginView"
              xmlns:vm="clr-namespace:AppDocenteAsignatura.ViewModels"
             Title="LoginView"  Shell.NavBarIsVisible="False" NavigationPage.HasNavigationBar="False" BackgroundColor="Black">
    <ContentPage.BindingContext>
        <vm:DocenteAsignaturaViewModel></vm:DocenteAsignaturaViewModel>
    </ContentPage.BindingContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.80*"></RowDefinition>
            <RowDefinition Height="0.20*"></RowDefinition>
        </Grid.RowDefinitions>
    <FlexLayout  Grid.Row="0"     Direction="Column" Margin="20">
        <Label HorizontalOptions="Center">Inicio de sesion</Label>
        <Label>Correo:</Label>
        <Entry   ></Entry>
        <Label>Contraseña:</Label>
        <Entry></Entry>

        </FlexLayout  >
        <Button Command="{Binding LoginCommand}" Grid.Row="1" TextColor="White" BorderColor="White" BackgroundColor="Transparent"   Text="Ingresar"    HorizontalOptions="Center" VerticalOptions="Center"></Button>
    </Grid>
</ContentPage>